@using PITB.CMS_Common
@{
    string a = Request.Url.Authority;
    string lObjDate = DateTime.Now.Date.ToString("MMMM dd, yyyy");
}

<div id="PrintableDiv">
    <link rel="stylesheet" type="text/css" href="http://@a/Content/bootstrap.css">
    <link rel="stylesheet" type="text/css" href="http://@a/Content/bootstrap.min.css">
    <link rel="stylesheet" type="text/css" href="http://@a/Content/DataTables/css/dataTables.bootstrap.css">
    <link rel="stylesheet" type="text/css" href="http://@a/Content/DataTables/css/responsive.bootstrap.css">
    <link rel="stylesheet" type="text/css" href="http://@a/css/crm.css">

    <style type="text/css">
        .table > thead > tr > th,
        .table > tbody > tr > th,
        .table > tfoot > tr > th {
            background-color: rgb(65, 94, 97);
            color: rgb(255,255,255);
        }

        .table > thead > tr > th,
        .table > tfoot > tr > th,
        .table > thead > tr > td,
        .table > tbody > tr > td,
        .table > tfoot > tr > td {
            page-break-inside: avoid;
        }

        .table > thead > tr {
            page-break-inside: avoid;
        }

        .table > tbody > tr {
            page-break-inside: avoid;
        }

        .table > tfoot > tr {
            page-break-inside: avoid;
        }
        /*.table > thead { display: table-header-group; }
                        .table thead, .table tfoot { display: table-row-group; }*/
    </style>
    @*<div class="z-btn">
        <div class="box-header-blue with-border btn-header">
            <h4 class="box-title">Category Wise Dashboard - @ViewBag.DistrictName District</h4>
        </div>

        <div class="BarMainCategory">
            @{ Html.RenderAction("ShowCategoryInBarChart", "Report", new { campId = ViewBag.CampaignIds, graphName = ViewBag.GraphName, userId = ViewBag.UserId, categoryType = (int)ViewBag.CategoryType, categoryId = ViewBag.CategoryId, graphLevel = 0, divTag = 4 }); }
        </div>
    </div>
    <div class="z-btn">
        <div class="box-header-blue with-border btn-header">
            <h4 class="box-title text-centered">Subordinates</h4>
        </div>
        @{
            Html.RenderAction("PieLegendChartReportForDistrictReport", "Report", new { campaignId = ViewBag.CampaignIds, divTag = 5, graphName = "other" });
        }
    </div>*@
    <div id="pageHeader" class="box-body-white clearfix">
        <div style="display:compact;">
            <img src="http://@a/img/PITB-LOGO.gif" style="max-width:70px; max-height:70px; margin-top: -3px; margin-right: 15px;position:absolute" />
            <p style="position:relative; text-align:center;margin:0px"><b>SED Hotline (042-111-11-20-20)</b></p>
            <p style="position:relative; text-align:center;margin:0px"><b>District Complaints Summary <i>as of <span id="docDate">@DateTime.Now.Date.ToString("D")</span></i></b></p>
            <p style="position:relative;text-align:center;margin:0px"><b> District of @ViewBag.DistrictName</b></p>
        </div>
    </div>
    <div class="BarMainCategory">
        @{
            Html.RenderAction("DistrictReportComplaintsSummary", "Report", new { divName = "Overdue", campId = ViewBag.CampaignIds, hierarchyId = ViewBag.HierarchyId, userHierarchyId = ViewBag.UserHierarchyId, commaSepVal = ViewBag.commaSepVal, statusIds = "6", reportType = (int)Config.SummaryReportType.General, divTag = "3" });
        }
    </div>
    <div class="BarMainCategory">
        @{
            Html.RenderAction("DistrictReportComplaintsSummary", "Report", new { divName = "ReOpened", campId = ViewBag.CampaignIds, hierarchyId = ViewBag.HierarchyId, userHierarchyId = ViewBag.UserHierarchyId, commaSepVal = ViewBag.commaSepVal, statusIds = "7", reportType = (int)Config.SummaryReportType.General, divTag = "2" });
        }
    </div>
    <div class="DistrictComplaints">
        @{
            Html.RenderAction("DistrictReportComplaintsSummary", "Report", new { divName = "Open", campId = ViewBag.CampaignIds, hierarchyId = ViewBag.HierarchyId, userHierarchyId = ViewBag.UserHierarchyId, commaSepVal = ViewBag.commaSepVal, statusIds = "1", reportType = (int)Config.SummaryReportType.General, divTag = "1" });
        }
    </div>
</div>